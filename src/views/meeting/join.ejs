<%- include('../layout', { 
    title: 'Join Meeting',
    body: `
<div class="container">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="card">
                <div class="card-header">
                    <h3><i class="bi bi-box-arrow-in-right"></i> Join Meeting</h3>
                </div>
                <div class="card-body">
                    ${error ? `<div class="alert alert-danger">${error}</div>` : ''}
                    <form method="POST" action="/meeting/join">
                        <div class="mb-3">
                            <label class="form-label">Meeting Code</label>
                            <input type="text" class="form-control" name="code" placeholder="Enter meeting code" required>
                        </div>
                        <button type="submit" class="btn btn-primary w-100">Join</button>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>
` }) %>

